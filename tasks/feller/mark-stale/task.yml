---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: cfbuildpacks/feature-eng-ci
    tag: minimal

inputs:
- name: ci
- name: release

run:
  path: ci/tasks/feller/mark-stale/task.sh

params:
  TRACKER_PROJECT:
  TRACKER_TOKEN:
  GITHUB_TOKEN:
